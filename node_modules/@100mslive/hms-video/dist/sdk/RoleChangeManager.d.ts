import { HMSRole } from '../interfaces';
import InitialSettings from '../interfaces/settings';
import { HMSUpdateListener } from '../interfaces/update-listener';
import ITransport from '../transport/ITransport';
import { IStore } from './store';
export declare type PublishConfig = {
    publishAudio?: boolean;
    publishVideo?: boolean;
};
export default class RoleChangeManager {
    private store;
    private transport;
    private publish;
    private removeAuxiliaryTrack;
    private listener?;
    constructor(store: IStore, transport: ITransport, publish: (settings: InitialSettings, publishConfig?: PublishConfig) => void, removeAuxiliaryTrack: (trackId: string) => void, listener?: HMSUpdateListener | undefined);
    handleLocalPeerRoleUpdate: ({ oldRole, newRole }: {
        oldRole: HMSRole;
        newRole: HMSRole;
    }) => Promise<void>;
    private removeVideoTracks;
    private removeAudioTrack;
    private removeScreenTracks;
    private removeAuxTracks;
    private removeTrack;
}
